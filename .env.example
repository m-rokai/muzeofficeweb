# Google Analytics 4 measurement ID (for example, G-XXXXXXXXXX).
NEXT_PUBLIC_GA_ID=

# Google Search Console HTML-tag verification token only (not the full tag).
NEXT_PUBLIC_GSC_TOKEN=

# Google Business Profile Place ID used by the post-booking review prompt.
NEXT_PUBLIC_G_PLACE_ID=

# Pooled Postgres connection URL. Run migrations/001_create_email_leads.sql
# before enabling the workspace-guide email capture in production.
DATABASE_URL=
